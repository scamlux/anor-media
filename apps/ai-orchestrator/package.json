{
  "name": "@anor/ai-orchestrator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "build": "echo 'Python service has no JS build step'",
    "lint": "echo 'No lint configured for ai-orchestrator'",
    "test": "echo 'No tests configured for ai-orchestrator'"
  }
}
