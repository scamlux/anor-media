global:
  scrape_interval: 15s

scrape_configs:
  - job_name: backend
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:4000"]

  - job_name: ai_orchestrator
    metrics_path: /health
    static_configs:
      - targets: ["ai-orchestrator:8000"]
